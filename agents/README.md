# Agents

从小职责开始。建议初始顺序：`java-generator`、`code-reviewer`、`log-diagnostician`、`customer-service`、`order-assistant`、`refund-advisor`、`knowledge-retriever`、`human-handoff`。

每个目录均遵循 `docs/04-architecture.md` 中的最小 Agent 合同，未通过评估的原型请标注为 `experimental`。
